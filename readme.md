# Tweaking commits: The Beginning

### Instalando o Commitizen Globalmente:
`npm install commitizen -g`

### Instalando o Commitizen no projeto:
`commitizen init cz-conventional-changelog --yarn --dev --exact`
